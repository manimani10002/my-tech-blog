# 게시글 작성 규칙

이 문서는 `content/posts`에 저장하는 모든 기술 게시글의 기준입니다. 초안을 수정하거나 발행 전 검토할 때 이 문서를 기준으로 삼습니다.

## 1. 파일과 주소

- 게시글은 `content/posts` 아래에 Markdown 파일 하나로 작성합니다.
- 파일명은 영문 소문자, 숫자, 하이픈만 사용합니다. 예: `next-routing-note.md`
- 파일명은 게시글 주소가 되므로 내용을 짐작할 수 있고, 발행 후에는 가능하면 바꾸지 않습니다.
- 규칙 문서나 메모를 `content/posts`에 두지 않습니다. 해당 폴더의 모든 `.md` 파일은 게시글로 읽힙니다.

## 2. Front matter

모든 게시글은 본문보다 먼저 다음 형식의 front matter를 둡니다.

```yaml
---
title: '게시글 제목'
description: '목록과 검색에 표시할 한 문장 요약'
date: '2026-09-07'
tags: ['Next.js', '개발']
draft: false
---
```

- `title`: 글의 핵심 결과나 주제를 드러내는 제목으로 작성합니다. 불필요한 수식어와 느낌표는 줄입니다.
- `description`: 독자가 글에서 얻을 내용을 한 문장으로 씁니다. 제목을 그대로 반복하지 않습니다.
- `date`: 실제 발행일을 `YYYY-MM-DD` 형식으로 씁니다. 존재하지 않는 날짜나 미래의 임의 날짜를 사용하지 않습니다.
- `tags`: 글의 주제를 나타내는 문자열 배열입니다. 보통 1~4개를 사용하고 같은 글 안에서 중복하지 않습니다.
- `draft: true`: 작성 중인 글입니다. 검토가 끝나 발행할 때 `false`로 바꾸거나 줄을 삭제합니다.
- 필수 필드(`title`, `description`, `date`, `tags`)를 빠뜨리거나 YAML 문법을 바꾸지 않습니다.

## 3. 본문 구성

본문은 제목을 다시 쓰지 않고, 짧은 도입 뒤 다음 흐름을 우선합니다.

1. **문제 또는 배경**: 무엇이 불편했고 왜 이 글을 쓰는지 설명합니다.
2. **해결 과정**: 선택한 방법을 단계별로 설명합니다.
3. **코드 또는 예시**: 설명과 직접 연결되는 최소 예시를 보여 줍니다.
4. **주의점과 한계**: 언제 통하지 않는지, 어떤 트레이드오프가 있는지 적습니다.
5. **결론**: 핵심을 요약하고 다음에 적용할 행동을 남깁니다.

모든 글이 다섯 항목을 반드시 가져야 하는 것은 아니지만, 독자가 `왜 → 어떻게 → 무엇을 주의할지`를 따라갈 수 있어야 합니다.

## 4. 문장과 Markdown

- 한 문단에는 하나의 생각만 담고, 문단 사이에는 빈 줄을 둡니다.
- 소제목은 `##`, 하위 소제목은 `###`부터 시작합니다. 본문에서 `#` 제목은 사용하지 않습니다.
- 처음 등장하는 기술 용어는 필요한 경우 짧게 풀어 씁니다.
- 명령어, 파일명, 변수명, 짧은 코드는 인라인 코드로 감쌉니다.
- 여러 줄 코드는 언어명을 붙인 fenced code block을 사용합니다. 예: ` ```typescript `
- 코드에는 설명에 필요한 부분만 남기고, 실행에 필요한 import나 입력값은 생략하지 않습니다.
- 목록, 표, 인용문, 체크리스트는 정보 구조가 분명해질 때 사용합니다.
- 이미지는 `public/images`에 두고 `/images/파일명.png` 경로로 연결합니다. 대체 텍스트를 반드시 작성합니다.
- 현재 렌더러가 지원하지 않는 MDX 문법이나 HTML 의존 표현은 사용하지 않습니다.

## 5. 기술 내용의 기준

- 사실, 버전, 실행 결과와 개인적인 의견을 구분합니다.
- 재현 가능한 명령어와 코드는 현재 프로젝트의 의존성과 맞춰 작성합니다.
- 외부 자료를 참고했다면 링크와 참고 시점을 남깁니다.
- 보안상 위험한 값, 실제 토큰, 개인정보를 예시에 포함하지 않습니다.
- 실패한 시도도 결론에 도움이 되면 무엇이 실패했고 왜 바꿨는지 기록합니다.
- 다른 사람의 글이나 코드를 그대로 옮기지 않고, 필요한 경우 출처와 라이선스를 확인합니다.

## 6. 초안 수정 규칙

초안을 다듬을 때는 다음 순서로 수정합니다.

1. 글의 주제와 독자를 한 문장으로 정리하고, 제목과 `description`이 이를 반영하는지 확인합니다.
2. 주장마다 근거, 코드, 실행 결과 또는 작성자의 경험이 연결되는지 확인합니다.
3. 중복 문장과 장황한 도입을 줄이고, 소제목 순서를 `배경 → 해결 → 검증 → 한계 → 결론`에 맞춥니다.
4. 원래 의미와 사실을 임의로 바꾸지 않습니다. 불확실한 내용은 삭제하거나 `[확인 필요]`로 표시합니다.
5. 예시 코드의 언어 태그, 파일 경로, 명령어와 본문의 설명이 서로 일치하는지 확인합니다.
6. 발행 가능한 문체로 정리한 뒤 front matter와 발행 전 체크리스트를 검토합니다.

## 7. 발행 전 체크리스트

- [ ] 파일명이 영문 소문자, 숫자, 하이픈으로만 되어 있는가?
- [ ] `title`, `description`, `date`, `tags`가 모두 있고 형식이 올바른가?
- [ ] 제목과 요약이 글의 실제 내용과 일치하는가?
- [ ] 도입, 핵심 설명, 예시, 주의점, 결론의 흐름이 자연스러운가?
- [ ] 코드와 명령어를 실제로 실행하거나 동작 여부를 확인했는가?
- [ ] 링크, 이미지 경로, 이미지 대체 텍스트가 유효한가?
- [ ] 비밀값과 개인정보가 없는가?
- [ ] 초안이면 `draft: true`, 발행 글이면 `draft: false`인가?
- [ ] `npm run lint`와 `npm run build`가 통과하는가?

## 8. 기본 게시글 템플릿

````markdown
---
title: ''
description: ''
date: 'YYYY-MM-DD'
tags: []
draft: true
---

## 배경

무엇을 해결하려는지 설명합니다.

## 해결 방법

핵심 개념과 과정을 설명합니다.

```text
필요한 예시를 작성합니다.
```
````

## 확인한 결과

실행 결과와 주의점을 기록합니다.

## 정리

핵심 내용을 짧게 요약합니다.

```

```
